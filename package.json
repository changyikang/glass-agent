{
  "name": "glass-mcp",
  "version": "1.2.0",
  "description": "配眼镜指南 MCP Server",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "glass-mcp": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "web": "npm run build && node dist/web.js",
    "dev": "tsc --watch",
    "test": "npm run build && node --test dist/**/*.test.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.0.0"
  }
}
